<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="dev.imranr.obtainium">
    <application
        android:label="@string/label"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/tv_banner"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true"
        android:localeConfig="@xml/locales_config"
        android:memtagMode="async">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action
                    android:name="com.android_package_installer.content.SESSION_API_PACKAGE_INSTALLED"
                    android:exported="false" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="obtainium" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false" />
        <!-- shiroikuma-kakutoku fork: the 保存復元 state-export contract.
             No android:permission — the caller (白い熊's 自由作業盤) cannot hold
             one, so the automation token is the gate. -->
        <receiver
            android:name=".StateExportReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="${applicationId}.action.EXPORT_STATE" />
                <action android:name="${applicationId}.action.LIST_CATEGORIES" />
                <!-- Cancel must come in through the EXPORTED receiver: a
                     third-party caller cannot reach a non-exported service,
                     so the stop path is signalled internally from here. -->
                <action android:name="${applicationId}.action.CANCEL_EXPORT" />
            </intent-filter>
        </receiver>
        <!-- shiroikuma-kakutoku fork, contract v2 §2a: the data door.
             The receiver above is the UNAUTHENTICATED half of the surface —
             it only ever writes where it was told to and reports what it did.
             Everything that moves data through a caller-supplied descriptor
             lives here instead, because a broadcast cannot tell you who sent
             it and this can: AutomationCallers checks the caller's exact
             package name, cross-checks the uid the kernel reports, and pins
             its signing certificate. No android:permission — a custom one
             would have to be `normal` (any app may hold it) or `signature`
             (impossible: every app in this family has its own keystore). -->
        <provider
            android:name=".automation.AutomationProvider"
            android:authorities="${applicationId}.automation"
            android:exported="true" />
        <!-- Not exported: a caller reaches it only through the provider. -->
        <service
            android:name=".automation.AutomationDataService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Automation data export/import" />
        </service>
        <!-- Capability discovery, readable WITHOUT waking this app: 応用管理
             builds its list across every installed package at once and must
             answer "can this app be backed up" for one that is currently
             frozen, which 白い熊 does a lot — a frozen app cannot be asked
             anything. `format` is skExportVersion (the family ZIP) and
             `min_format` the oldest archive skReadExportFile still decodes. -->
        <meta-data android:name="shiroikuma.automation.contract"   android:value="2" />
        <meta-data android:name="shiroikuma.automation.format"     android:value="2" />
        <meta-data android:name="shiroikuma.automation.min_format" android:value="1" />
        <meta-data
            android:name="dev.imranr.obtainium.service.NOTIFICATION_ICON"
            android:resource="@drawable/ic_notification" />
    </application>
    <uses-feature android:name="android.hardware.touchscreen" android:required="false"/>
    <uses-feature android:name="android.hardware.touchscreen.multitouch" android:required="false"/>
    <uses-feature android:name="android.software.leanback" android:required="false"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.UPDATE_PACKAGES_WITHOUT_USER_ACTION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.ENFORCE_UPDATE_OWNERSHIP"/>
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <!-- shiroikuma-kakutoku fork: the 保存復元 automation may be told to write
         its backup ZIP to an arbitrary absolute directory (the `path` extra),
         which needs All-files access. Without the grant the export falls back
         to the SAF folder configured on the 白い熊 獲得 UI page. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <!-- shiroikuma-kakutoku fork: the Shizuku client permissions. A Shizuku
         server decides which installed packages are its clients by scanning
         requestedPermissions, and the two servers look for different names:
         白い熊 雫 (shiroikuma.shizuku) accepts af.shizuku.plus.* and defines it
         itself, while stock Shizuku accepts only moe.shizuku.manager.* (and
         deliberately owns that name, so 白い熊 雫 cannot also define it —
         INSTALL_FAILED_DUPLICATE_PERMISSION). The moe.* line already arrives
         by manifest merge from the Shizuku provider AAR; ours is declared
         explicitly so a permission that actually EXISTS on the device is
         requested, which is what lets the server's grant land instead of
         failing silently. A uses-permission naming a permission no installed
         package defines is inert, so having both costs nothing. -->
    <uses-permission android:name="af.shizuku.plus.permission.API_V23" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- shiroikuma-kakutoku fork: goes with AutomationDataService's
         foregroundServiceType="specialUse" (required from API 34). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- shiroikuma-kakutoku fork, contract v2 §4: BOTH callers, and the
             second one is the one that has actually been calling. Without a
             package here, a reply broadcast's setPackage fails SILENTLY on
             Android 11+ — the export runs, writes correctly, and is never
             heard of — and the data door's caller check cannot read the
             caller's signing certificate either, so a legitimate call is
             refused as "signature unreadable" rather than merely losing its
             reply. 応用管理 drives the data door of §2a; 自由作業盤 drives the
             保存復元 batch of §1. QUERY_ALL_PACKAGES above already grants the
             visibility; these are declared anyway so it does not depend on a
             permission that could be dropped. -->
        <package android:name="shiroikuma.oyokanri" />
        <package android:name="shiroikuma.jiyusagyoban" />
    </queries>
</manifest>
